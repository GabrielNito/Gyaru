# Neon Postgres (Vercel)
# DATABASE_URL = pooled connection (for migrations, Prisma CLI)
# DATABASE_URL_UNPOOLED = direct connection (for Prisma adapter at runtime)

DATABASE_URL="postgresql://user:password@ep-xxx-xxx.pooler.us-east-2.aws.neon.tech/gyaru?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://user:password@ep-xxx-xxx.us-east-2.aws.neon.tech/gyaru?sslmode=require"
